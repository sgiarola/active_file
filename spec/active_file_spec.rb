require 'spec_helper'

describe ActiveFile do
  it 'has a version number' do
    expect(ActiveFile::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
